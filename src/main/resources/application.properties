spring.application.name=demo
server.port=8080
spring.jackson.time-zone=UTC

# Firebase configuration
firebase.database.url=${FIREBASE_DATABASE_URL:https://veterinaria-app-a2d50.firebaseio.com}
firebase.config.path=${FIREBASE_CONFIG_PATH:firebase-service-account.json}
firebase.api.key=${FIREBASE_API_KEY:AIzaSyCCeWOCwHr-1tfOyG0dST5yHXFjCnDI-n8}

# Security configuration
spring.security.user.name=omar@email.com
spring.security.user.password=sape1234

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.example.demo=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT configuration
jwt.secret=${JWT_SECRET:miClaveSecretaJWTGenerada1234567890abcdef}
jwt.expiration=${JWT_EXPIRATION:3600000}

# Mailtrap configuration
spring.mail.host=${SPRING_MAIL_HOST:sandbox.smtp.mailtrap.io}
spring.mail.port=${SPRING_MAIL_PORT:2525}
spring.mail.username=${SPRING_MAIL_USERNAME:4d5bcd53f4c00b}
spring.mail.password=${SPRING_MAIL_PASSWORD:3cdf86cb105621}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Swagger configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.syntaxHighlight.activated=true